rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Local emulator: allow read/write for development. Restrict in production.
      allow read, write: if true;
    }
  }
}
